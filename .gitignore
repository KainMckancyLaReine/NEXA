# Secrets — never commit
.env
.env.local

# Dependencies
node_modules/

# Runtime demo database — regenerated by the server on start, not source
server/data/nexa.json

# OS / editor junk
.DS_Store
**/.DS_Store
*.save
.fuse_hidden*
*.log
